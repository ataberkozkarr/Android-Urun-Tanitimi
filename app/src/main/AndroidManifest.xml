<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.works.saticiOL">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/eshop"
        android:label="@string/app_name"
        android:roundIcon="@drawable/eshop"
        android:supportsRtl="true"
        android:theme="@style/Theme.saticiOL">
        <activity
            android:name="com.works.saticiOL.activities.ProductDetail"
            android:exported="false" />
        <activity
            android:name="com.works.saticiOL.activities.ProductShow"
            android:exported="false" />
        <activity
            android:name="com.works.saticiOL.activities.Register"
            android:exported="false" />
        <activity
            android:name="com.works.saticiOL.activities.Login"
            android:exported="false" />
        <activity
            android:name="com.works.saticiOL.activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>